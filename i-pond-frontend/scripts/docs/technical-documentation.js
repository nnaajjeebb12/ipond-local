const fs = require("fs");
const path = require("path");
const { Packer } = require("docx");
const {
  p, h1, h1First, h2, h3, bullet, code, screenshot, table,
  titlePage, tocSection, makeDoc,
} = require("./_helpers");

const TITLE = "Technical Documentation";

const children = [
  ...titlePage(TITLE, "Developer Handover"),
  ...tocSection(TITLE),

  h1First("1. Project Setup"),
  h2("Prerequisites"),
  bullet("Node.js 20 LTS or newer."),
  bullet("Docker Desktop (for the TimescaleDB container)."),
  bullet("A package manager (npm 10+)."),
  h2("Install Steps"),
  ...code([
    "# 1. Clone and install",
    "git clone <repo-url>",
    "cd i-pond-frontend",
    "npm install --legacy-peer-deps   # next-auth beta needs --legacy-peer-deps",
    "",
    "# 2. Bring up the database",
    "docker compose up -d",
    "# migrations in db/migrations/ run automatically on first start.",
    "",
    "# 3. Configure .env (see Environment Variables section).",
    "",
    "# 4. Start dev server",
    "npm run dev",
    "# open http://localhost:3000",
  ].join("\n")),

  h1("2. Architecture"),
  h2("App Router Layout"),
  p("All routes live under src/app/ using the Next.js 16 App Router. Page files are page.tsx, server actions are in src/app/actions/, API handlers are route.ts files under src/app/api/. Every API route exports `runtime = \"nodejs\"` and `dynamic = \"force-dynamic\"` because we need access to the pg pool and never want cached responses."),
  h2("Server vs Client"),
  bullet("Pages are React Server Components by default; \"use client\" only for interactive widgets (charts, forms, modals)."),
  bullet("Session is read on the server via auth() in route handlers; the client reads through Providers (NextAuth SessionProvider)."),
  bullet("Data fetching from client components uses SWR with `/api/...` endpoints."),
  h2("File Map"),
  table(
    ["Path", "Role"],
    [
      ["src/app/api/", "All route handlers (auth + tenant-scoped reads, ingest, admin CRUD)."],
      ["src/app/dashboard/", "All-ponds, per-pond, per-sensor pages."],
      ["src/app/admin/", "Admin console pages (users, ponds, logs)."],
      ["src/lib/", "Cross-cutting helpers: db pool, admin guard, alert engine, pond status."],
      ["src/components/", "Re-usable widgets (SensorCard, AlertPopup, charts, layouts)."],
      ["src/hooks/", "SWR-backed hooks (useApi, useAlerts, useMaintenance, useThresholds, useDashboardStats, useSystemHealth)."],
      ["src/store/", "Zustand auth store for client-side mirror of session."],
      ["src/auth.ts / src/auth.config.ts", "next-auth v5 server config and edge-safe callbacks."],
    ],
    { widths: [3400, 5960] },
  ),

  h1("3. Key Libraries"),
  table(
    ["Library", "Version", "Used For"],
    [
      ["next", "16.2.3", "App Router, route handlers, server actions, image opt."],
      ["next-auth", "5.0.0-beta.25", "Auth: credentials provider, JWT sessions, middleware."],
      ["bcryptjs", "^2.4.3", "Password hashing in /api/auth and /api/admin/users."],
      ["pg", "^8.13.1", "PostgreSQL driver, configured as a pooled singleton in src/lib/db.ts."],
      ["swr", "^2.4.1", "Client-side data fetching and polling."],
      ["zustand", "^5.0.12", "Tiny store for auth mirror."],
      ["uplot", "^1.6.32", "Canvas charts for dashboards and reports."],
      ["jspdf + jspdf-autotable", "^4.2.1 + ^5.0.7", "Client-side PDF export."],
      ["moment", "^2.30.1", "Timezone-aware date formatting."],
      ["axios", "^1.15.0", "Outbound HTTP."],
      ["react-hook-form", "^7.72.1", "Forms in admin and settings pages."],
      ["next-themes", "^0.4.6", "Light/dark mode."],
      ["tailwindcss", "^4", "Styling."],
    ],
    { widths: [2200, 1600, 5560] },
  ),

  h1("4. Authentication"),
  h2("NextAuth Configuration"),
  p("src/auth.ts wires the Credentials provider. authorize() looks up owners by LOWER(email), compares the password with bcrypt.compare, and returns { id, email, name, role } on success. The JWT callback in src/auth.config.ts copies id and role into the token; the session callback copies them back to session.user. Result: session.user.role is available everywhere."),
  h2("Session Shape"),
  ...code([
    "session = {",
    "  user: {",
    "    id: '8d3e...uuid',",
    "    email: 'alice@example.com',",
    "    name:  'Alice',",
    "    role:  'admin' | 'owner' | 'viewer',",
    "  },",
    "  expires: '...'",
    "}",
  ].join("\n")),
  h2("Role Checks"),
  bullet("requireAdmin() in src/lib/admin.ts returns { ok, userId } for admins and { ok: false, res } for everyone else — call it at the top of admin-only route handlers."),
  bullet("Owner/viewer routes call await auth() directly and use ownsPond(userId, pondId) helpers (see /api/readings, /api/thresholds)."),

  h1("5. Database"),
  h2("TimescaleDB"),
  p("sensor_readings is a TimescaleDB hypertable partitioned by time, with the default chunk interval (7 days). All other tables are plain Postgres."),
  h2("Running Migrations"),
  p("Migrations live in db/migrations/ numbered 001..011 and are mounted into the TimescaleDB container at /docker-entrypoint-initdb.d. They run only on first start of the container. To re-apply manually:"),
  ...code([
    "# Re-run a single migration",
    "docker exec -i soletronix-timescaledb \\",
    "  psql -U $POSTGRES_USER -d $POSTGRES_DB < db/migrations/004_optimal_ranges.sql",
    "",
    "# Reset everything (DESTRUCTIVE)",
    "docker compose down -v",
    "docker compose up -d",
  ].join("\n")),

  h1("6. API Conventions"),
  bullet("Every handler exports runtime = 'nodejs' and dynamic = 'force-dynamic'."),
  bullet("Auth: call await auth() first; respond 401 if no session."),
  bullet("Tenant scoping: non-admins must pass through ownsPond() or a JOIN on user_pond_access."),
  bullet("Errors: short JSON tags like 'invalid_payload', 'invalid_sensor', 'forbidden', 'db_error' with appropriate HTTP status."),
  bullet("Body validation: parse JSON inside try/catch, validate every field before SQL."),
  bullet("Bulk writes use BEGIN/COMMIT with a pooled client (see /api/thresholds PATCH)."),
  bullet("All SQL uses parameterized queries via the pg pool — no string concatenation of user input."),

  h1("7. Ingestion Pipeline"),
  p("src/app/api/send-sensor-data/route.ts is the only public-write endpoint. Steps:"),
  bullet("Read X-Forwarded-For or X-Real-IP into `ip` for logging."),
  bullet("Validate Authorization header equals `Bearer ${process.env.API_TOKEN}`. On mismatch: 401 + log."),
  bullet("Parse JSON body. On parse error: 400 invalid_json + log."),
  bullet("Validate body.data.pnd is finite and between 1 and 10. Translate to pond_code PND-XXX."),
  bullet("SELECT id FROM ponds WHERE pond_code = $1. Missing row: 404 unknown_pond + log."),
  bullet("INSERT into sensor_readings with time = NOW()."),
  bullet("INSERT a heartbeat row into pond_status_log with status='online' (drives utilization)."),
  bullet("Call runAlertChecks(pondId, values) — may create sensor_alerts row."),
  bullet("Always write an ingestion_logs row with raw payload, IP, http_status, error_message."),

  h1("8. Real-time Pattern"),
  p("The dashboard refreshes with SWR. Key choices:"),
  bullet("/api/ponds/status polls every 30 seconds for the dot color."),
  bullet("/api/readings?range=today appends new points using the `since` query parameter; older windows refetch in full."),
  bullet("SWR's refreshWhenHidden is disabled so polling pauses when the tab is hidden, reducing server load."),
  bullet("AlertPopup hooks into useAlerts which polls /api/alerts/active and renders unacknowledged rows as toasts."),
  bullet("Notification bell uses /api/notifications/unread-count with a short interval."),

  h1("9. Shared Utilities"),
  bullet("src/lib/db.ts — singleton pg Pool, max=10, idleTimeoutMillis 30s. Reuses one pool across hot reloads."),
  bullet("src/lib/admin.ts — requireAdmin() guard."),
  bullet("src/lib/alerts.ts — runAlertChecks(pondId, values) + per-sensor checkForAlert()."),
  bullet("src/lib/pondStatus.ts — getPondStatus(lastSeenMs, hasMaintenance) returns 'online' | 'stale' | 'offline' | 'maintenance'. Also exports STATUS_DOT_BG, STATUS_DOT_GLOW, STATUS_LABEL, and fmt(v) that 2-decimal-rounds a number or returns '—'."),
  bullet("APP_TIMEZONE env var controls the timezone used by time_bucket() in /api/readings."),

  h1("10. Adding a New Page"),
  ...code([
    "// 1. Create the route file:",
    "// src/app/insights/page.tsx",
    "import { auth } from '@/auth';",
    "import { redirect } from 'next/navigation';",
    "",
    "export default async function InsightsPage() {",
    "  const session = await auth();",
    "  if (!session?.user) redirect('/login');",
    "",
    "  return (",
    "    <main className=\"p-6\">",
    "      <h1 className=\"text-2xl font-semibold\">Insights</h1>",
    "      {/* client widgets go here */}",
    "    </main>",
    "  );",
    "}",
    "",
    "// 2. Add a link in MainLayout.tsx so users can navigate to it.",
    "// 3. If admin-only, wrap with requireAdmin or check role inline.",
  ].join("\n")),

  h1("11. Adding a New API Route"),
  ...code([
    "// src/app/api/insights/route.ts",
    "import { NextResponse } from 'next/server';",
    "import { auth } from '@/auth';",
    "import { pool } from '@/lib/db';",
    "",
    "export const runtime = 'nodejs';",
    "export const dynamic = 'force-dynamic';",
    "",
    "export async function GET() {",
    "  const session = await auth();",
    "  if (!session?.user) {",
    "    return NextResponse.json({ error: 'unauthorized' }, { status: 401 });",
    "  }",
    "",
    "  const isAdmin = session.user.role === 'admin';",
    "  const { rows } = await pool.query(",
    "    isAdmin",
    "      ? 'SELECT ... FROM ... ORDER BY ...'",
    "      : 'SELECT ... FROM ... JOIN user_pond_access upa ON ... WHERE upa.user_id = $1',",
    "    isAdmin ? [] : [session.user.id]",
    "  );",
    "",
    "  return NextResponse.json(rows);",
    "}",
  ].join("\n")),

  h1("12. Common Gotchas"),
  bullet("Humidity has been removed everywhere — do not add it back to the ESP32 payload, DB, or UI."),
  bullet("Timestamps are TIMESTAMPTZ in UTC. Convert to APP_TIMEZONE only for display."),
  bullet("Round sensor values in SQL: ROUND(::numeric, 2)::float8 — do not round on the client."),
  bullet("Get pond status from src/lib/pondStatus.ts — never duplicate the 1-minute / 3-minute logic."),
  bullet("Optimal ranges come from pond_sensor_thresholds — never hardcode."),
  bullet("Modals use React Portals (createPortal) to avoid Tailwind stacking-context issues — do not nest modals deep inside layout components."),
  bullet("next-auth v5 is in beta; npm install needs --legacy-peer-deps."),
  bullet("Sensor alerts re-trigger after acknowledgement (migration 010) — do not assume \"one alert per pond per sensor\"."),
  bullet("ESP32 firmware sends rtd, ph, sal, dox — different from the DB column names. Do not change the firmware mapping."),

  h1("13. Environment Variables"),
  table(
    ["Variable", "Required", "Description"],
    [
      ["DATABASE_URL", "yes", "Postgres connection string."],
      ["POSTGRES_USER", "yes", "Container superuser, read by docker-compose.yml."],
      ["POSTGRES_PASSWORD", "yes", "Container password."],
      ["POSTGRES_DB", "yes", "Container DB name."],
      ["API_TOKEN", "yes", "Bearer token for /api/send-sensor-data."],
      ["AUTH_SECRET", "yes", "next-auth JWT signing secret (32 bytes)."],
      ["APP_TIMEZONE", "no", "IANA tz for time_bucket(), default UTC."],
      ["NEXTAUTH_URL", "no", "Base URL in production (e.g. https://ipond.example.com)."],
      ["NODE_ENV", "no", "'development' or 'production'."],
    ],
    { widths: [2400, 1200, 5760] },
  ),

  h1("14. Deployment"),
  p("Recommended deployment splits the frontend from the database:"),
  bullet("Frontend on Vercel: `vercel link && vercel deploy --prod`. Set every env var in the Vercel dashboard."),
  bullet("Database on a dedicated VPS or managed Postgres with TimescaleDB extension. Restore from migrations/ on first run."),
  bullet("Expose the DB to Vercel through Cloudflare Tunnel or Tailscale — never open Postgres to the public internet."),
  bullet("ESP32 gateways POST to the public Next.js URL with the Bearer token, so they only need outbound HTTPS."),
  bullet("Set ${process.env.DATABASE_URL} on Vercel to the tunneled DB endpoint."),
  bullet("Verify by hitting /api/auth/signin and /api/admin/logs once deployed."),
];

const doc = makeDoc(children, TITLE);

const out = path.join(__dirname, "..", "..", "docs", "Technical-Documentation.docx");
Packer.toBuffer(doc).then((buf) => {
  fs.writeFileSync(out, buf);
  console.log("wrote", out);
});
